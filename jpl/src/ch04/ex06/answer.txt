(a)
木のノードを表すTreeNode ... 以下の前提に
変更前の前提 : 木のノードを表す ... 具象クラス
変更後の前提 : ノードとしてデータの保持に加えて、Accessor以外の役割が与えられた場合(例えば、探索時の比較方法をTreeNodeで実装する場合)... 抽象クラス

情報の保持、Accessor部分を実装し、与えられた役割を実行する部分を抽象メソッドとして定義、TreeNodeを継承したサブクラス内で、役割に応じて抽象メソッドを実装する。

(b)
探索アルゴリズムを定めたTreeWalker

変更前の前提 : 探索アルゴリズムを定めた ... インタフェース
変更後の前提 : 探索対象であるTreeNodeが保持する値の比較方法が確定している場合 ... 抽象クラス

TreeNodeを比較する部分を実装する。
比較後、どのTreeNodeに進むかを、具象クラス側で定義する。

(c)
描画可能なオブジェクトのためのDrawable

変更前の前提 : 描画可能なオブジェクトのため ... 抽象クラス
変更後の前提 : ???

GUI表示部分は、TreeNodeで取り扱う変数によって変化することが考えられるため、ここは抽象化する。
その他、描画に必要な部分は共通だと考えられるため、本クラス内で実装することがよいと考える。


(d)
GUIで実行に使用する Application

変更前の前提 : GUIで実行に使用する ... 抽象クラス
変更後の前提 : ???

GUIか否かの部分は必ず必要になるため、本クラス内で実装すべき。
OSごとの実行部分を抽象化し、具象クラスで実装するよう設計する。

